Main branch: no API version of the code. used for running the code during testing using serial comunication between the PI and a computer running the API

API Recovery 2.0 branch: a branch used to recover the API for the final program when we have an API capable of running on the PI.
